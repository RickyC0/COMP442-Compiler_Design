// TEST 6.1.15 / 10.3 [error] type error in return statement | EXPECT: ERROR

bad() : integer
    local
        float x;
    do
        x = 2.5; //Error here
        return (x);
    end

main
    local
        integer a;
    do
        a = bad();
    end
